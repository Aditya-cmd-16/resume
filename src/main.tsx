import { ChangeEvent, FormEvent, useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import { BrowserRouter, Link, NavLink, Route, Routes, useNavigate } from 'react-router-dom'
import { AlertCircle, ArrowRight, BarChart3, BriefcaseBusiness, CheckCircle2, ChevronRight, Download, FileText, LayoutDashboard, ListChecks, ScanSearch, Sparkles, Upload } from 'lucide-react'
import { Bar, BarChart, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import './styles.css'
import './overrides.css'

type Report = {
  id: string; role: string; filename?: string; createdAt: string; overall: number; ats: number; keyword: number; impact: number; formatting: number
  strengths: string[]; concerns: string[]; actions: string[]; matched: string[]; missing: string[]; sections: { name: string; status: 'strong' | 'review' | 'missing'; note: string }[]
}

const stopWords = new Set('a an the and or of in to for with on at by from as is are was were be been being this that these those it its your our we you i my have has had will would should could can role job work years year experience skills required preferred ability strong excellent using use team business company'.split(' '))
const actionVerbs = ['achieved', 'built', 'created', 'delivered', 'designed', 'developed', 'implemented', 'improved', 'increased', 'launched', 'led', 'managed', 'optimized', 'reduced', 'resolved', 'scaled', 'streamlined']

function terms(text: string) {
  return [...new Set((text.toLowerCase().match(/[a-z][a-z+#.-]{2,}/g) || []).filter(word => !stopWords.has(word) && !/^\d/.test(word)))].slice(0, 120)
}
function analyze(resume: string, role: string, jobDescription: string, filename?: string): Report {
  const normalized = resume.toLowerCase()
  const has = (words: string[]) => words.some(word => normalized.includes(word))
  const sections = [
    { name: 'Contact details', keys: ['email', 'linkedin', 'github', 'phone'] },
    { name: 'Professional summary', keys: ['summary', 'profile', 'objective'] },
    { name: 'Experience', keys: ['experience', 'employment', 'work history'] },
    { name: 'Education', keys: ['education', 'university', 'college', 'bachelor', 'master'] },
    { name: 'Skills', keys: ['skills', 'technologies', 'technical skills'] },
  ].map(section => ({ name: section.name, present: has(section.keys) }))
  const figures = (resume.match(/(?:\$|₹|\d)[\d,.]*(?:\s?%|\s?(?:k|m|million|users|clients|hours|days|people))?/gi) || []).length
  const verbs = actionVerbs.filter(verb => normalized.includes(verb)).length
  const jdTerms = jobDescription ? terms(jobDescription) : []
  const matched = jdTerms.filter(term => normalized.includes(term)).slice(0, 18)
  const missing = jdTerms.filter(term => !normalized.includes(term)).slice(0, 12)
  const repetition = terms(resume).filter(term => (normalized.match(new RegExp(`\\b${term.replace(/[.+#-]/g, '\\$&')}\\b`, 'g')) || []).length > 8).slice(0, 3)
  const sectionScore = Math.round(sections.filter(s => s.present).length / sections.length * 100)
  const impact = Math.min(100, 42 + figures * 7 + verbs * 5)
  const keyword = jobDescription ? Math.round(matched.length / Math.max(1, Math.min(jdTerms.length, 25)) * 100) : 55
  const formatting = Math.min(100, 55 + sectionScore / 2 + (resume.length > 600 ? 10 : 0))
  const ats = Math.round((sectionScore * .35) + (keyword * .35) + (formatting * .3))
  const overall = Math.round(ats * .55 + impact * .45)
  const strengths = [
    ...(sections.filter(s => s.present).length >= 4 ? ['Your resume includes most core ATS sections.'] : []),
    ...(figures >= 3 ? [`You use ${figures} measurable signals, which helps recruiters assess impact.`] : []),
    ...(matched.length >= 5 ? [`${matched.length} job-description terms appear in the resume.`] : []),
  ]
  const concerns = [
    ...(sections.filter(s => !s.present).map(s => `${s.name} is not clearly labeled, which can reduce ATS readability.`)),
    ...(figures < 3 ? ['Few quantified outcomes were detected. Add honest scale, time, quality, or business-result measures where available.'] : []),
    ...(jobDescription && missing.length ? [`${missing.length} relevant job-description terms are missing. Add only those you can substantiate.`] : []),
    ...(repetition.length ? [`Possible keyword repetition detected: ${repetition.join(', ')}. Avoid repeating terms without context.`] : []),
  ].slice(0, 5)
  const actions = [
    ...(figures < 3 ? ['Rewrite the two most recent experience bullets with a specific outcome and metric.'] : []),
    ...(jobDescription && missing.length ? [`Review these missing terms and incorporate truthful evidence where applicable: ${missing.slice(0, 5).join(', ')}.`] : []),
    ...(!has(['summary', 'profile']) ? [`Add a 2–3 line summary tailored to the ${role} role.`] : []),
    ...(!has(['skills', 'technical skills']) ? ['Add a focused skills section with tools and technologies you have used.'] : []),
  ].slice(0, 4)
  return { id: crypto.randomUUID(), role, filename, createdAt: new Date().toLocaleDateString(), overall, ats, keyword, impact, formatting, strengths: strengths.length ? strengths : ['The resume has usable content to build on.'], concerns: concerns.length ? concerns : ['No critical automated issues were detected; review role-specific language before applying.'], actions: actions.length ? actions : ['Tailor the summary and most relevant bullets to each job description.'], matched, missing, sections: sections.map(s => ({ name: s.name, status: s.present ? 'strong' : 'missing', note: s.present ? 'Clearly represented in the submitted text.' : 'Add or use a conventional section heading.' })) }
}

function Shell({ children }: { children: React.ReactNode }) {
  return <div className="app-shell"><aside className="sidebar"><Link className="logo" to="/"><span>✦</span> ResumeAI</Link><p className="workspace">WORKSPACE</p><nav><NavLink to="/" end><LayoutDashboard />Dashboard</NavLink><NavLink to="/analyze"><ScanSearch />Analyze resume</NavLink><NavLink to="/reports"><FileText />Reports</NavLink><NavLink to="/career"><BriefcaseBusiness />Career roadmap</NavLink></nav><p className="sidebar-note">Factual, ATS-first feedback. Your text stays in this browser.</p></aside><main className="content"><header><div><p className="eyebrow">CAREER INTELLIGENCE</p><h1>Make every application count.</h1></div><Link className="primary-button" to="/analyze"><Sparkles />New analysis</Link></header>{children}</main></div>
}

function Dashboard() { const reports = loadReports(); const latest = reports[0]; return <>{!latest && <section className="hero-card"><div><span className="pill"><Sparkles /> ATS-ready feedback</span><h2>Turn your experience into your next opportunity.</h2><p>Paste your resume, add a job description, and get a factual recruiter-style review of relevance, impact, keywords, and readability.</p><Link className="light-button" to="/analyze">Start an analysis <ChevronRight /></Link></div><div className="hero-score"><span>WHAT YOU GET</span><strong>ATS</strong><small>keyword and impact review</small></div></section>}<section className="metric-grid"><Metric icon={<ScanSearch />} label="ATS compatibility" value={latest ? `${latest.ats}%` : '—'} detail="Structure + relevance"/><Metric icon={<ListChecks />} label="Keyword coverage" value={latest ? `${latest.keyword}%` : '—'} detail="Against job description"/><Metric icon={<BarChart3 />} label="Impact signals" value={latest ? `${latest.impact}%` : '—'} detail="Results and action verbs"/></section>{latest && <section className="two-column"><div className="panel"><div className="panel-title"><div><p className="eyebrow">LATEST ANALYSIS</p><h3>{latest.role}</h3></div><Link to="/reports">Full report <ChevronRight /></Link></div><ScoreChart report={latest} /></div><div className="panel"><p className="eyebrow">NEXT BEST ACTIONS</p><h3>Improve application readiness</h3><ActionList items={latest.actions}/><Link className="text-link" to="/analyze">Analyze another version <ChevronRight /></Link></div></section>}</> }
function Metric({icon,label,value,detail}:{icon:React.ReactNode;label:string;value:string;detail:string}) { return <article className="metric"><span>{icon}</span><p>{label}</p><b>{value}</b><small>{detail}</small></article> }
function ScoreChart({report}:{report:Report}) { const data=[{name:'ATS',score:report.ats},{name:'Keywords',score:report.keyword},{name:'Impact',score:report.impact},{name:'Format',score:report.formatting}]; return <div className="chart-area"><ResponsiveContainer width="100%" height={220}><BarChart data={data} layout="vertical" margin={{left:8}}><XAxis type="number" domain={[0,100]} hide/><YAxis type="category" width={80} dataKey="name" tick={{fill:'#9bb0c7',fontSize:12}} axisLine={false} tickLine={false}/><Tooltip cursor={{fill:'#13233b'}}/><Bar dataKey="score" radius={[0,7,7,0]} fill="#62d9ff"/></BarChart></ResponsiveContainer></div> }
function ActionList({items}:{items:string[]}) { return <ul className="actions">{items.map(item=><li key={item}><CheckCircle2 />{item}</li>)}</ul> }

function Analyze() { const navigate=useNavigate(); const [text,setText]=useState(''); const [role,setRole]=useState(''); const [jd,setJd]=useState(''); const [fileName,setFileName]=useState(''); const [preview,setPreview]=useState(''); const [error,setError]=useState(''); function fileChange(e:ChangeEvent<HTMLInputElement>) { const file=e.target.files?.[0]; if(!file)return; setFileName(file.name); if(file.type.startsWith('image/')) { setPreview(URL.createObjectURL(file)); setError('Image attached. Paste the resume text below so the analysis stays factual; OCR can be added through a secure server.') } else if(file.type.startsWith('text/') || file.name.endsWith('.txt')) { file.text().then(setText); setPreview(''); setError('') } else { setPreview(''); setError('For reliable, factual analysis, paste the text from PDF/DOCX below. File parsing can be connected to a secure server later.') } } function submit(e:FormEvent) {e.preventDefault(); if(!text.trim()||!role.trim()) return; const report=analyze(text,role,jd,fileName); const reports=loadReports(); localStorage.setItem('resumeai-reports',JSON.stringify([report,...reports].slice(0,10))); navigate(`/reports/${report.id}`)} return <section className="analysis-page"><div className="page-intro"><p className="eyebrow">NEW ANALYSIS</p><h2>Get a recruiter-ready review.</h2><p>We only evaluate information you provide. A job description enables a precise keyword comparison.</p></div><form className="analyze-form" onSubmit={submit}><label className={`dropzone ${fileName?'has-file':''}`}>{preview ? <img className="file-preview" src={preview} alt="Attached resume preview"/> : <Upload/>}<b>{fileName || 'Attach a resume file (optional)'}</b><span>TXT, PDF, DOCX, JPG, or JPEG. Paste the resume text below for analysis.</span><input type="file" accept=".txt,.pdf,.doc,.docx,.jpg,.jpeg,image/jpeg" onChange={fileChange}/></label>{error && <p className="warning"><AlertCircle />{error}</p>}<div className="field"><label htmlFor="role">Target role <i>*</i></label><input id="role" value={role} onChange={e=>setRole(e.target.value)} placeholder="e.g., Data Analyst" required/></div><div className="field"><label htmlFor="resume">Resume text <i>*</i></label><textarea id="resume" value={text} onChange={e=>setText(e.target.value)} placeholder="Paste the complete text of your resume here…" rows={13} required/></div><div className="field"><label htmlFor="jd">Job description <em>Optional but recommended</em></label><textarea id="jd" value={jd} onChange={e=>setJd(e.target.value)} placeholder="Paste the target job description to identify matched and missing keywords." rows={7}/></div><button className="primary-button submit" disabled={!text.trim()||!role.trim()}><Sparkles />Analyze my resume</button><p className="form-note">This version runs locally in your browser and does not invent accomplishments.</p></form></section> }

function ReportView({report}:{report:Report}) { const pie=[{name:'Matched',value:report.matched.length},{name:'Missing',value:report.missing.length || 1}]; const ranking=report.overall>=80?'Strong':report.overall>=60?'Competitive':'Needs revision'; const download=()=>{const blob=new Blob([JSON.stringify({overall_score:report.overall,ats_score:report.ats,formatting_score:report.formatting,keyword_score:report.keyword,grammar_score:null,projects:[],strengths:report.strengths,weaknesses:report.concerns,missing_keywords:report.missing,recommendations:report.actions,final_verdict:ranking},null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='resumeai-report.json';a.click();URL.revokeObjectURL(url)}; return <section className="report-view"><div className="report-header"><div><span className="pill"><CheckCircle2 /> Analysis complete</span><h2>{report.role} analysis</h2><p>{report.filename || 'Pasted resume'} · {report.createdAt}</p></div><div className="report-actions"><button className="download-button" onClick={download}><Download/>JSON</button><div className="score-circle"><strong>{report.overall}</strong><span>overall readiness</span></div></div></div><div className="report-grid"><div className="panel"><p className="eyebrow">STRENGTHS</p><ActionList items={report.strengths}/></div><div className="panel"><p className="eyebrow">RECRUITER WATCHOUTS</p><ul className="actions concerns">{report.concerns.map(x=><li key={x}><AlertCircle />{x}</li>)}</ul></div><div className="panel wide"><div className="panel-title"><div><p className="eyebrow">ATS MATCHING</p><h3>{ranking} match · {report.ats}%</h3></div><span className="score-badge">{report.keyword}% keywords</span></div><p className="muted">This score reflects recognizable headings, job-description term coverage, and readable, achievement-focused language. It is a decision aid, not a guarantee of shortlisting.</p><div className="section-list">{report.sections.map(s=><div key={s.name}><span className={s.status}><CheckCircle2 /></span><b>{s.name}</b><p>{s.note}</p></div>)}</div></div><div className="panel"><p className="eyebrow">KEYWORD + SKILLS GAP</p>{report.matched.length || report.missing.length ? <><ResponsiveContainer width="100%" height={160}><PieChart><Pie data={pie} dataKey="value" innerRadius={44} outerRadius={65} paddingAngle={4}>{pie.map((_,i)=><Cell key={i} fill={i?'#344967':'#66e3b4'}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><p className="keyword-line"><b>Matching skills:</b> {report.matched.join(', ') || 'No job description provided'}</p><p className="keyword-line"><b>Must-have gaps to validate:</b> {report.missing.join(', ') || 'None detected'}</p></> : <p className="muted">Add a job description in the next analysis to compare skills and technologies.</p>}</div><div className="panel"><p className="eyebrow">PRIORITY ACTIONS</p><ActionList items={report.actions}/></div><div className="panel wide"><p className="eyebrow">PROJECT + CAREER REVIEW</p><div className="mini-grid"><article><b>Project evaluation</b><p>Projects are strongest when they name the problem, technology stack, your contribution, and a truthful metric. Add missing technologies only when you used them.</p><span>Project score: evidence required</span></article><article><b>Career recommendation</b><p>Current fit: {ranking.toLowerCase()} for {report.role}. Prioritize the validated skills gap, role-specific projects, and quantified experience.</p><span>Shortlisting probability: {Math.min(90,Math.max(15,report.overall-5))}% (indicative)</span></article><article><b>Interview preparation</b><p>Prepare examples for your most relevant project, a measurable achievement, a setback, and why this role. Practice explaining every claimed skill.</p><span>Suggested level: role-aligned</span></article></div></div></div></section> }
function loadReports():Report[] { try{return JSON.parse(localStorage.getItem('resumeai-reports')||'[]')}catch{return[]} }
function Reports() { const reports=loadReports(); const nav=useNavigate(); const [selected,setSelected]=useState(()=>location.pathname.split('/').pop()); const report=useMemo(()=>reports.find(x=>x.id===selected)||reports[0],[reports,selected]); if(!report)return <section className="empty-page"><FileText/><h2>No saved reports yet.</h2><p>Complete an analysis to save and compare reports here.</p><Link className="primary-button" to="/analyze">Analyze a resume</Link></section>; return <><div className="report-picker">{reports.map(x=><button key={x.id} className={x.id===report.id?'selected':''} onClick={()=>{setSelected(x.id);nav(`/reports/${x.id}`)}}>{x.role}<span>{x.overall}</span></button>)}</div><ReportView report={report}/></> }
function Career() { const report=loadReports()[0]; return <section className="empty-page"><BriefcaseBusiness/><h2>{report ? `Focus next on ${report.role}.` : 'Your career roadmap starts with your resume.'}</h2><p>{report ? report.actions[0] : 'Analyze a resume to receive role-fit, priority-skill, and learning recommendations.'}</p><Link className="primary-button" to="/analyze">{report?'Analyze another role':'Start analysis'} <ArrowRight/></Link></section> }
function App() { return <BrowserRouter><Shell><Routes><Route path="/" element={<Dashboard/>}/><Route path="/analyze" element={<Analyze/>}/><Route path="/reports" element={<Reports/>}/><Route path="/reports/:id" element={<Reports/>}/><Route path="/career" element={<Career/>}/></Routes></Shell></BrowserRouter> }
export default App

createRoot(document.getElementById('root')!).render(<App />)
